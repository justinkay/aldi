_BASE_: "../../Base-RCNN-FPN.yaml"
MODEL:
  ROI_HEADS:
    NUM_CLASSES: 8
DATASETS:
  TRAIN: ("cityscapes_train",)
  TEST: ("cityscapes_val", "cityscapes_foggy_val",)
SOLVER:
  BACKWARD_AT_END: False
OUTPUT_DIR: "output/cityscapes_baseline_nobackwardatend/"