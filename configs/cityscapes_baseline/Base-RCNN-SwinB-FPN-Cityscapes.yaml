_BASE_: "../_Base-RCNN-SwinB-FPN.yaml"
MODEL:
  ROI_HEADS:
    NUM_CLASSES: 8
# This is similar to the setting used in Mask R-CNN paper, Appendix A
# But there are some differences, e.g., we did not initialize the output
# layer using the corresponding classes from COCO
INPUT:
  MIN_SIZE_TRAIN: (800, 832, 864, 896, 928, 960, 992, 1024)
  MIN_SIZE_TRAIN_SAMPLING: "choice"
  MIN_SIZE_TEST: 1024
  MAX_SIZE_TRAIN: 2048
  MAX_SIZE_TEST: 2048
DATASETS:
  TRAIN: ("cityscapes_train",)
  TEST: ("cityscapes_val", "cityscapes_foggy_val",)
SOLVER:
  BASE_LR: 0.005
  STEPS: (36000,)
  MAX_ITER: 48000
  IMS_PER_BATCH: 4
  AMP:
    ENABLED: True
  WARMUP_FACTOR: 0.0008 # 1/1250
  WARMUP_ITERS: 1250
  # just save a few checkpoints throughout because we will also use BestCheckpointer
  # every TEST.EVAL_PERIOD
  CHECKPOINT_PERIOD: 12500
  # OPTIMIZER: "ADAMW"
TEST:
  EVAL_PERIOD: 1250
VIS_PERIOD: 1250
OUTPUT_DIR: "output/cityscapes_swinb_baseline/"